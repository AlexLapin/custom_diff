require 'spec_helper'

describe CustomDiff do
  it 'has a version number' do
    expect(CustomDiff::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
